{
  "name": "manage",
  "private": true,
  "scripts": {
    "start:test": "turbo run start:test",
    "start:ot": "turbo run start:ot",
    "start:online": "turbo run start:online",
    "build": "turbo run build",
    "build:test": "turbo run build:test",
    "build:ontest": "turbo run build:ontest",
    "build:online": "turbo run build:online",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky install",
    "clean": "bash ./scripts/clean.sh"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.6.7",
    "@commitlint/config-conventional": "^17.6.7",
    "eslint": "^8.57.0",
    "husky": "^8.0.0",
    "prettier": "^3.2.5",
    "turbo": "^2.2.3",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
